# Компонент "Форма обратной связи" с расширенной обработкой файлов

## Поддерживаемые поля

* **S - Строка:**
  * input (строк = 1)
  * textarea (строк > 1)
* **N - Число**
* **L - Список:**
  * select - (список / флажки)
  * checkbox - (флажки)
* **S:DateTime - Дата/Время**
* **E - Привязка к элементам**
* **F - Файл** *(с расширенной валидацией и защитой)*

## Новые возможности (v2.0)

### 🔒 Безопасность и защита
- ✅ **CSRF защита** через check_bitrix_sessid()
- ✅ **Rate Limiting** - ограничение частоты отправки (5/мин, 30/час, 100/день)
- ✅ **Magic Bytes валидация** - проверка реального типа файла
- ✅ **Расширенная валидация файлов** - поиск вредоносного кода
- ✅ **Система карантина** - изоляция подозрительных файлов
- ✅ **Многоуровневая безопасность** - настраиваемые уровни проверки

### 🎨 Современный UX/UI
- ✅ **Drag & Drop интерфейс** - удобная загрузка файлов
- ✅ **Предпросмотр файлов** - миниатюры изображений
- ✅ **Мобильная оптимизация** - адаптивный дизайн
- ✅ **Анимации и переходы** - плавные эффекты
- ✅ **Accessibility поддержка** - совместимость со скрин-ридерами

### 📊 Аналитика и отслеживание
- ✅ **Google Analytics 4** - события, конверсии, Enhanced Ecommerce
- ✅ **Яндекс.Метрика** - цели, параметры визита
- ✅ **Facebook Pixel** - отслеживание лидов
- ✅ **VK Pixel** - ретаргетинг
- ✅ **Milestone события** - прогресс заполнения формы

### 🏗️ Архитектурные улучшения
- ✅ **Система хуков** - расширяемость через события
- ✅ **Разделение логики** - отдельные классы для разных задач
- ✅ **Асинхронная обработка** - неблокирующие операции
- ✅ **Улучшенное логирование** - детальное отслеживание

## Пример базового вызова

```php
<?php
$APPLICATION->IncludeComponent(
    "ddapp.tools:forms",
    ".default",
    array(
        // Основные настройки
        "IBLOCK_ID" => 49,
        "EMAIL_TEMPLATE" => "DDAPP_MESSAGE_FORM",
        "BUTTON_TEXT" => "Связаться с нами",
        
        // Безопасность
        "USE_BITRIX_CAPTCHA" => "N",
        "USE_GOOGLE_RECAPTCHA" => "Y",
        "GOOGLE_RECAPTCHA_PUBLIC_KEY" => "your_site_key",
        "GOOGLE_RECAPTCHA_SECRET_KEY" => "your_secret_key",
        "RATE_LIMIT_ENABLED" => "Y",
        
        // Файлы
        "MAX_FILE_SIZE" => 15,
        "SECURITY_LEVEL" => "medium",
        
        // Аналитика
        "ENABLE_ANALYTICS" => "Y",
        "GA_MEASUREMENT_ID" => "G-XXXXXXXXXX",
        "YANDEX_METRIKA_ID" => "12345678",
        
        "CACHE_TYPE" => "A",
        "CACHE_TIME" => "3600"
    ),
    false
);
?>
```

## Пример расширенного вызова

```php
<?php
$APPLICATION->IncludeComponent(
    "ddapp.tools:forms",
    ".default",
    array(
        // Основные настройки
        "IBLOCK_ID" => 49,
        "EMAIL_TEMPLATE" => "DDAPP_MESSAGE_FORM",
        "BUTTON_TEXT" => "Отправить заявку",
        "ENABLE_ANALYTICS" => "Y",

        // Настройки защиты
        "USE_BITRIX_CAPTCHA" => "N",
        "USE_GOOGLE_RECAPTCHA" => "Y",
        "GOOGLE_RECAPTCHA_PUBLIC_KEY" => "6LdXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
        "GOOGLE_RECAPTCHA_SECRET_KEY" => "6LdXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
        "RATE_LIMIT_ENABLED" => "Y",
        "RATE_LIMIT_PER_MINUTE" => "3",
        "RATE_LIMIT_PER_HOUR" => "20",

        // Настройки файлов
        "MAX_FILE_SIZE" => 25, // MB
        "ALLOWED_FILE_EXTENSIONS" => "jpg,jpeg,png,pdf,doc,docx,xls,xlsx",
        "FILE_UPLOAD_DIR" => "/upload/forms/",
        "CHECK_FILE_CONTENT" => "Y",
        "ANTIVIRUS_CHECK" => "N",
        "SECURITY_LEVEL" => "high",

        // Аналитика
        "GA_MEASUREMENT_ID" => "G-XXXXXXXXXX",
        "YANDEX_METRIKA_ID" => "12345678",
        "FACEBOOK_PIXEL_ID" => "123456789012345",
        "VK_PIXEL_ID" => "VK-RTRG-XXXXXX-XXXXX",

        // Кеширование
        "CACHE_TYPE" => "A",
        "CACHE_TIME" => "3600"
    ),
    false
);
?>
```

## Настройки компонента

### Основные параметры

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `IBLOCK_ID` | int | ID инфоблока для создания формы | - |
| `EMAIL_TEMPLATE` | string | ID шаблона письма | - |
| `BUTTON_TEXT` | string | Текст кнопки открытия формы | "Открыть форму" |
| `ENABLE_ANALYTICS` | Y/N | Включить систему аналитики | Y |

### Параметры защиты

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `USE_BITRIX_CAPTCHA` | Y/N | Использовать Bitrix Captcha | N |
| `USE_GOOGLE_RECAPTCHA` | Y/N | Использовать Google reCAPTCHA v3 | N |
| `GOOGLE_RECAPTCHA_PUBLIC_KEY` | string | Публичный ключ reCAPTCHA | - |
| `GOOGLE_RECAPTCHA_SECRET_KEY` | string | Секретный ключ reCAPTCHA | - |
| `RATE_LIMIT_ENABLED` | Y/N | Включить ограничение частоты | Y |
| `RATE_LIMIT_PER_MINUTE` | int | Максимум отправок в минуту | 5 |
| `RATE_LIMIT_PER_HOUR` | int | Максимум отправок в час | 30 |

### Параметры файлов

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `MAX_FILE_SIZE` | int | Максимальный размер файла в MB | 10 |
| `ALLOWED_FILE_EXTENSIONS` | string | Разрешенные расширения через запятую | jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx,txt,zip |
| `FILE_UPLOAD_DIR` | string | Папка для загрузки файлов | /upload/ddapp_forms/ |
| `CHECK_FILE_CONTENT` | Y/N | Проверять содержимое файлов | Y |
| `ANTIVIRUS_CHECK` | Y/N | Антивирусная проверка | N |
| `SECURITY_LEVEL` | string | Уровень безопасности (low/medium/high) | medium |

### Параметры аналитики

| Параметр | Тип | Описание | По умолчанию |
|----------|-----|----------|--------------|
| `GA_MEASUREMENT_ID` | string | Google Analytics Measurement ID | - |
| `YANDEX_METRIKA_ID` | string | ID счетчика Яндекс.Метрики | - |
| `FACEBOOK_PIXEL_ID` | string | Facebook Pixel ID | - |
| `VK_PIXEL_ID` | string | VK Pixel ID | - |

## Структура файлов компонента

```
/components/ddapp.tools/forms/
├── .description.php              # Описание компонента
├── .parameters.php              # Параметры компонента  
├── class.php                   # Основная логика
├── lang/ru/class.php           # Языковые файлы
├── classes/                    # Дополнительные классы
│   ├── FileSecurityValidator.php   # Валидация файлов
│   ├── RateLimiter.php            # Ограничение частоты
│   └── HookManager.php            # Система хуков
├── config/
│   └── file_security.php      # Конфигурация безопасности
└── templates/.default/
    ├── template.php           # Шаблон формы
    ├── script.js             # JavaScript
    ├── script.min.js         # Минифицированный JS
    ├── style.css             # Стили
    └── style.min.css         # Минифицированные стили
```

## Система хуков

### Доступные события

- `beforeValidation` - перед валидацией данных
- `afterValidation` - после валидации данных
- `beforeSave` - перед сохранением в инфоблок
- `afterSave` - после сохранения в инфоблок
- `beforeEmailSend` - перед отправкой письма
- `afterEmailSend` - после отправки письма

### Пример создания хука

```php
use DDAPP\Tools\Components\Forms\FormHook;

class CustomFormHook implements FormHook
{
    public function beforeSave($data, $formId)
    {
        // Кастомная обработка данных перед сохранением
        return $data;
    }

    public function afterSave($elementId, $data, $formId)
    {
        // Отправка в CRM, уведомления и т.д.
        return $elementId;
    }
    
    // Другие методы интерфейса...
}

// Регистрация хука
$component = $APPLICATION->IncludeComponent(...);
$hookManager = $component->getHookManager();
$hookManager->addHook('beforeSave', new CustomFormHook());
```

## Аналитика

### Отслеживаемые события

#### Google Analytics 4
- `form_opened` - открытие формы
- `form_progress` - заполнение полей
- `form_milestone` - достижение 25%, 50%, 75%
- `form_submit` - успешная отправка
- `form_error` - ошибки валидации
- `file_upload` - загрузка файлов
- `form_abandoned` - закрытие без отправки

#### Яндекс.Метрика
- `form_view` - просмотр формы
- `form_submit` - отправка формы
- `form_error` - ошибка в форме

### Настройка Enhanced Ecommerce

```javascript
// Автоматически отправляется при успешной отправке
gtag('event', 'form_submit', {
    'form_id': 'ddapp_form_49',
    'form_name': 'Форма обратной связи',
    'element_id': 12345,
    'fill_time_seconds': 45,
    'value': 1,
    'currency': 'RUB'
});
```

## Безопасность файлов

### Уровни безопасности

#### Low (Низкий)
- Базовая проверка расширений
- Проверка размера файлов

#### Medium (Средний) - по умолчанию
- Проверка MIME-типов
- Проверка содержимого файлов
- Карантин подозрительных файлов

#### High (Высокий)
- Magic Bytes валидация
- Строгая проверка MIME-типов
- Антивирусное сканирование
- Удаление метаданных

### Автоматические проверки

1. **Расширение файла** - белый и черный список
2. **MIME-тип** - соответствие расширению
3. **Magic Bytes** - реальный тип файла
4. **Размер файла** - ограничение по размеру
5. **Имя файла** - отсутствие опасных символов
6. **Содержимое** - поиск вредоносного кода
7. **Двойные расширения** - защита от file.txt.php

### Запрещенные типы файлов

```php
"forbidden_extensions" => [
    // Исполняемые файлы
    "exe", "bat", "cmd", "com", "scr", "pif", "vbs", "vbe",
    // Веб-скрипты
    "php", "php3", "php4", "php5", "phtml", "asp", "aspx",
    "jsp", "pl", "py", "rb", "cgi",
    // Системные файлы
    "dll", "sys", "drv", "ocx",
    // Макросы
    "xlsm", "xlsb", "xltm", "docm", "dotm", "pptm"
]
```

## API для разработчиков

### Получение экземпляра формы

```javascript
// Получение менеджера формы
var formManager = window.getDDAppForm_49(); // где 49 - ID инфоблока

// Программная отправка формы
formManager.submitForm();

// Получение данных формы
var formData = formManager.getFormData();

// Добавление кастомного валидатора
formManager.addCustomValidator(function(formData) {
    // Кастомная логика валидации
    return true;
});
```

### События в JavaScript

```javascript
// Отслеживание событий формы
document.addEventListener('ddapp:form:opened', function(e) {
    console.log('Форма открыта:', e.detail.formId);
});

document.addEventListener('ddapp:form:submitted', function(e) {
    console.log('Форма отправлена:', e.detail);
});
```

## Примеры использования в письме

В шаблоне письма доступны переменные:

```
#ELEMENT_ID# - ID созданного элемента
#IBLOCK_ID# - ID инфоблока  
#DATE_CREATE# - Дата создания заявки
#PROPERTY_CODE# - Значения свойств по их символьному коду
```

Для файловых полей в письме будут указаны имена загруженных файлов.

## Мобильная оптимизация

### Автоматические улучшения
- **Touch targets** - минимум 44px для кнопок
- **Viewport handling** - адаптация под виртуальную клавиатуру
- **Gesture support** - swipe для закрытия модалки
- **Auto-zoom prevention** - font-size: 16px для input
- **Responsive images** - адаптивные превью файлов

### Accessibility
- **ARIA labels** - полная поддержка скрин-ридеров
- **Keyboard navigation** - Tab/Enter навигация
- **Focus management** - видимые индикаторы фокуса
- **Color contrast** - соответствие WCAG 2.1
- **Reduced motion** - учет prefers-reduced-motion

## Логирование

Компонент ведет подробные логи через `LogHelper`:

- Успешные отправки форм
- Ошибки валидации
- Отклоненные файлы
- Файлы в карантине
- Rate limiting события
- Аналитические события

## Производительность

### Оптимизации
- **Lazy loading** - загрузка скриптов по требованию
- **Кеширование** - свойства инфоблока и валидация
- **Минификация** - сжатые CSS/JS файлы
- **Асинхронные операции** - неблокирующая обработка
- **CDN ready** - поддержка внешних ресурсов

### Мониторинг
```javascript
// Метрики производительности автоматически отправляются в GA
gtag('event', 'timing_complete', {
    'name': 'form_load_time',
    'value': loadTime
});
```

## Требования

- **PHP 7.4+**
- **Bitrix Framework 20.0+**
- **Модуль "Информационные блоки"**
- **Модуль "Почтовые события"**
- Для антивирусных проверок: **ClamAV** (опционально)
- Для полной аналитики: **Google Analytics 4**, **Яндекс.Метрика**

## Совместимость

- ✅ **Desktop**: Chrome 80+, Firefox 75+, Safari 13+, Edge 80+
- ✅ **Mobile**: iOS 13+, Android 8+
- ✅ **Screen readers**: NVDA, JAWS, VoiceOver
- ✅ **Frameworks**: Bootstrap 5.x, совместим с другими CSS фреймворками

## Changelog

### v2.0.0 (Current)
- ➕ Добавлена система хуков и событий
- ➕ CSRF защита и Rate Limiting
- ➕ Drag & Drop интерфейс загрузки файлов
- ➕ Полная мобильная оптимизация
- ➕ Интеграция с Google Analytics 4 и Яндекс.Метрикой
- ➕ Magic Bytes валидация файлов
- ➕ Расширенное логирование
- ➕ Асинхронная обработка формы
- 🔧 Рефакторинг архитектуры
- 🔧 Улучшенная безопасность файлов

### v1.0.0
- ➕ Базовая функциональность формы
- ➕ Поддержка всех типов полей инфоблока
- ➕ Базовая валидация файлов
- ➕ Bitrix Captcha и Google reCAPTCHA
- ➕ Отправка почтовых уведомлений